# Pokyny pre Claude

- Po zmene v `resources/js|css/**`, Vue/Blade (Tailwind), `tailwind.config.js`, `vite.config.js`, `package.json` alebo v API volanom frontendom (`routes/**`, controllery) spusti bez pýtania `npm run build`.
- `public/build` je verzovaný v gite a patrí do toho istého commitu ako zmena, ktorá ho vyvolala.
